apiVersion: v1
kind: ReplicationController
metadata:
  name: rc-coffee
spec:
  replicas: 3
  selector:
    env: dev
  template:
    metadata:
      name: tmp-coffee
      labels:
        env: dev
    spec:
      containers:
        - name: c1
          image: nginx
          ports:
            - containerPort: 80
